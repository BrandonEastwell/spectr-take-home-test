An RFQ is considered tradeable when its status is Quoted, it has both a bid and offer price, and it has not expired. 
Stale quotes (>30 seconds old) remain tradeable but are visually highlighted because the requirements only specify a 
freshness warning rather than preventing acceptance.